services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://localhost:3000}
      - PORT=4000
    ports:
      - "${CLIENT_PORT:-80}:80"
